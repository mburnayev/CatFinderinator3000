# This file was auto-generated by the Firebase CLI, and slightly modified by me
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on: 
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CATFINDERINATOR3000 }}
          channelId: live
          projectId: catfinderinator3000
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
